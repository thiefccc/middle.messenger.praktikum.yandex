[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: any unknown path is served by the bundled index.html
# (the client-side router will handle the actual route).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
